name = "bioh3nr1d14z-presence"
main = "src/index.ts"
compatibility_date = "2025-04-01"

# Gói free chỉ hỗ trợ Durable Object dùng storage backend SQLite, nên bắt buộc
# là `new_sqlite_classes` chứ không phải `new_classes`. Class này không đụng
# tới storage, nhưng backend vẫn phải khai báo đúng.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["PresenceCounter"]

[[durable_objects.bindings]]
name = "PRESENCE"
class_name = "PresenceCounter"
